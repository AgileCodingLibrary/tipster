@model IEnumerable<FazTipster.Entities.Tips>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AndyTipsterTips)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IrishHorseTips)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UltimateTips)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AndyTipsterTips)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IrishHorseTips)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UltimateTips)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
